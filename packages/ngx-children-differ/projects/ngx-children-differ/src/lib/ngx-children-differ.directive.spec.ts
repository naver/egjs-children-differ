import { NgxChildrenDifferDirective } from './ngx-children-differ.directive';

describe('NgxChildrenDifferDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxChildrenDifferDirective();
    expect(directive).toBeTruthy();
  });
});
